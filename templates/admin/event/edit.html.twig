{% extends 'admin/base.html.twig' %}

{% block title %}Edit Event
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Éditer l'événement</h1>

		{{ include('admin/event/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

		<a class="btn btn-warning" href="{{ path('admin_event_index') }}">Retour à la liste</a>

		{{ include('admin/event/_delete_form.html.twig') }}
	</div>
{% endblock %}
