{% extends 'base.html.twig' %}

{% block title "Voir categorie" %}
{% block description "Details administrateur categorie" %}

{% block body %}
{% include "admin/_admin_nav.html.twig" %}
<div class="container">
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_category_index') }}">back to list</a>

    <a href="{{ path('admin_category_edit', {'id': category.id}) }}">edit</a>

    {{ include('admin_category/_delete_form.html.twig') }}
</div>
{% endblock %}
